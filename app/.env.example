# RecordShelf 本机环境变量示例。请复制为 .env 并填写自己的值。
# 不要把真实密钥提交到 Git。

# Apple Music 官方编辑介绍（专辑详情页的「专辑介绍」模块）需要一个
# MusicKit Developer Token：用 Apple Developer 账户的 MusicKit 私钥
# （.p8）以 ES256 签出的 JWT，payload 含 iss=Team ID、iat、exp，
# header 含 kid=Key ID。令牌只在本机服务端使用。
#
# 也可以不用环境变量，改为写入下面这个私人文件（权限 0600）：
#   ~/Library/Application Support/RecordShelf/apple-music-developer-token
APPLE_MUSIC_DEVELOPER_TOKEN=

# AI 聆听指南的服务密钥同样只保存在本机，详见
# docs/LISTENING_GUIDE_SPEC.md。
# OPENAI_API_KEY=
# GEMINI_API_KEY=
